.text-center
  = form_for @topic do |f|
    = f.label :name
    = f.text_field :name
    %br
    = f.submit 'Save', class: 'btn btn-primary'
    = link_to 'Back', topics_path, class: 'btn btn-primary'
